/*
 * Module dependencies
 */
var angular = require("angular");

/*
 * Expose the module
 */
module.exports = angular.module("app.services", []);

// Initialize
require("./version");
require("./user");
